<%= form_for @topic do |f| %>
  <% if @topic.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@topic.errors.count, "error") %> prohibited
        this proposal from being saved:
      </h2>
      <ul>
        <% @topic.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <p>
</p>
     <p>
    <%= f.label :name %><br>
    <%= f.text_field :name, :value => current_user.name  %>
  </p>
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.label :proposal %><br>
    <%= f.text_area :proposal %>
  </p>

  <p>
<!--    <%= f.submit "Submit", class: "btn btn-primary btn-lg" %> -->
  </p>
<h2 style="color:red;"> Register your Vote</h2>
<p>
  Please vote on your proposal by selecting "For, Against, Undecided or N/a
   ( for cases where a vote is not appropriate)".  Then click the submit button.
</p>
<p>
  This is the best place for the proposer to vote, other members will be asked
   to do so elsewhere.
</p>
   <p>
    <div style='width: 400px; margin: auto'><%=f.label :"#{current_user.name}_vote".to_sym%></div>
    <div style='width: 200px; margin: auto'><%=f.select  "#{current_user.name}_vote".to_sym, options_for_select(@votes) %></div>
  </p>
<p>
    <div style='width: 400px; margin: auto'><%= f.submit "Submit Proposal & Vote", class: "btn btn-primary btn-lg" %></div>
</p>
<%end%>
